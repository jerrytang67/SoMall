import { Store, StoreConfig } from '@datorama/akita';
import { Injectable } from '@angular/core';

export interface {{ properCase name }}State {

}

export function createInitialState(): {{ properCase name }}State {
    return {  };
}

@Injectable({ providedIn: 'root' })
@StoreConfig({ name: '{{ camelCase name }}' })
export class {{ properCase name }}Store extends Store<{{ properCase name }}State> {
    constructor() {
        super(createInitialState());
    }
}